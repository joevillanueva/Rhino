source 'https://rubygems.org'

gem 'rails', '3.2.8'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3'


# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
gem 'bcrypt-ruby', '~> 3.0.0' # [2] This line gets uncommented and saved.
# [3] Afterwards, run a bundle install in Terminal.
# [4] Once completed, scaffold a new model for your users in Terminal.
# [4] We ran 'rails g scaffold User name:string username:string email:string password_digest:string'
# [4] What this line does is create a model called User, with columns of name, username, email, and password_digest.
# [4.5] It also specifies the data type of each column, and gives you some basic controller, routes, methods, and actions for your Rails app.  
# [4.5] Run a rake db:migrate to get your database up to date.
# [5] Now lets head to: your_app_name/app/models/model_name (in this case, replace model_name with user.rb)

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
